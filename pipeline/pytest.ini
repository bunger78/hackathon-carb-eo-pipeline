[pytest]
pythonpath = .
addopts = -m "not golden"
markers =
    integration: talks to real GCP; excluded in CI
    golden: live golden-set eval against Firestore; run explicitly with -m golden
testpaths = tests
